#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/*/
struct content{};
struct queue{};

// the RLE encoding in p1a for writing into memory
struct content
my_zip(){};

// producer thread
void *producer();
// consummer thread
void *consumer();

/*/
void
RLE_encoding(FILE *fp, int i, int argc);
